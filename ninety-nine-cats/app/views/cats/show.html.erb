<h2>Cat Show</h2>
<%# <%= render 'cat', cat: @cat, action: :show %>

<table> 
    <thead>
        <tr>
            <th><%= @cat.name %>'s Info</th>
        </tr>
    </thead>
    <tbody>
        <tr>
            <td><%= @cat.birth_date %></td>
            <td><%= @cat.name %></td>
            <td> <%= @cat.color %></td>
            <td><%= @cat.sex %></td>
        </tr>
    </tbody>
</table>
